

addEventListener("keydown", a => {
    const keyPress = a.key;
    const codigoKey = a.keyCode;
    const masKey = a.code;

    
   
    console.log(keyPress);
    console.log(masKey);
     console.log(codigoKey);
});
